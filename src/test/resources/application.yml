spring:
  batch:
    job:
      enabled: false
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  profiles:
    active:
    - test
thread:
  count: 1